import React from 'react'
import { TitleContainer } from './Title.elements';

function Title({ title }) {
    return (
        <TitleContainer>
            {title}
        </TitleContainer>
    )
}

export default Title